/**
 * App shell: routing and global providers will be mounted here.
 * Providers: Theme, Auth, Socket, QueryClient (to be added in Phase 1–2).
 */
export function AppShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

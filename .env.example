# FlowBoard — copy to .env and fill in values. Never commit .env.

# Supabase (when using Supabase Auth + DB)
# VITE_SUPABASE_URL=https://xxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Backend API (when using custom Node backend)
# VITE_API_URL=http://localhost:3001
